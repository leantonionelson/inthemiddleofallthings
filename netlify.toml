[build]
  command = "npm run build"
  publish = "dist"
  # Note: Firebase API keys are intentionally public (client-side config)
  # Netlify's secrets scanner detects placeholder values in Firebase's node_modules type definitions
  # To fix: Add SECRETS_SCAN_SMART_DETECTION_ENABLED=false in Netlify dashboard environment variables

